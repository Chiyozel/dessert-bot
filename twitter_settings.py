import oauth2 as oauth

screen_name='_LEDESSERT_' # Twitter username
token = oauth.Token(key="", secret="") # Twitter user token and secret
consumer = oauth.Consumer(key="", secret="") # Twitter appliction key and secret
